
let ch = 'a';
if (ch >= 'A' && ch <= 'Z') {
    document.write("ch is upercase"+"<br>");
} else if (ch >= 'a' && ch <= 'z') {
    document.write("ch is lowercase"+"<br>");
} else {
    document.write("not character"+"<br>");
}